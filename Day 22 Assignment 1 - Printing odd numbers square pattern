#include <stdio.h>

int main() {
    int r, c, i = 1;

    for(r = 1; r <= 3; r++) {
        for(c = 0; c < 4; c++) {
            printf("%d\t", i);
            i += 2;
        }
        printf("\n");
        i -= 4;
    }

    return 0;
}
