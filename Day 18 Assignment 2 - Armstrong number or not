#include<stdio.h>
int main()
{
    int n,r,temp=0,sum=0;
    scanf("%d",&n);
    temp=n;
    while(n!=0)
    {
        r = n % 10;
        sum = sum + (r*r*r);
        n = n / 10;
    }
    if(temp == sum)
    printf("ARMSTRONG");
    else
    printf("NOT ARMSTRONG");
    return 0;
}
